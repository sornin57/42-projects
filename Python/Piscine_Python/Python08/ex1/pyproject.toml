[tool.poetry]
name = "matrix-loading"
version = "0.1.0"
description = "Dependency management exercise"
authors = ["msornin"]

[tool.poetry.dependencies]
python = "^3.10"
pandas = "*"
numpy = "*"
matplotlib = "*"
requests = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
