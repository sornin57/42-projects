# Cercle 01

Projects from circle 01.

## Projects

- `get_next_line` - reads and returns one line at a time from a file descriptor.
- `ft_printf` - recreates the basic behavior of the standard `printf` function.
